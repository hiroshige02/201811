<%= form_with(model: equipment_event, local: true) do |form| %>
  <% if equipment_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(equipment_event.errors.count, "error") %> prohibited this equipment_event from being saved:</h2>

      <ul>
      <% equipment_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.number_field :event_id %>
  </div>

  <div class="field">
    <%= form.label :equipment_id %>
    <%= form.number_field :equipment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
