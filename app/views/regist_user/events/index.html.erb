<!-- 主催者の開催予定はユーザーのマイページで一覧として確認。
本ページ不要-->

<p id="notice"><%= notice %></p>

<h1>Events</h1>

<table>
  <thead>
    <tr>
      <th>Start time</th>
      <th>Finish time</th>
      <th>Title</th>
      <th>Regist user</th>
      <th>Content</th>
      <th>Admin ok</th>
      <th>Admin message</th>
      <th>Admin no</th>
      <th>Participand</th>
      <th>Event image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.start_time %></td>
        <td><%= event.finish_time %></td>
        <td><%= event.title %></td>
        <td><%= event.regist_user_id %></td>
        <td><%= event.content %></td>
        <td><%= event.admin_ok %></td>
        <td><%= event.admin_message %></td>
        <td><%= event.admin_no %></td>
        <td><%= event.participand %></td>
        <td><%= event.event_image_id %></td>
        <td><%= link_to 'Show', event %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event', new_event_path %>
